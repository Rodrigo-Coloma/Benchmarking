El secret postgres_password.txt va aquí (no se commitea — ver .gitignore).
Crea el archivo con:
  echo -n "tu_password" > deploy/secrets/postgres_password.txt
  chmod 600 deploy/secrets/postgres_password.txt
